#include<iostream>
using namespace std;
int main(){
    int a[]={3,5,9,89,23,67};
    int max=a[0];
    int min=a[0];
    int len=sizeof(a)/sizeof(a[0]);
    for(int i=0;i<len;i++){
        if(a[i]>max){
            max=a[i];
        }
        if(a[i]<min){
            min=a[i];
        }
    }
    cout<<max<<endl;
    cout<<min<<endl;
}
