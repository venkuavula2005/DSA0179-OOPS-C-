#include<iostream>
#include<algorithm>
using namespace std;
int main(){
    int a[]={-45,78,23,89,-90};
    int len=sizeof(a)/sizeof(a[0]);
    sort(a,a+len);
    for(int i=0;i<len;i++){
        cout<<a[i]<<" ";
    }
}
