#include<iostream>
using namespace std;
int main(){
    int a[]={1,2,3,4,5},sum=0,i;
    int len=sizeof(a)/sizeof(a[0]);
    for(i=0;i<5;i++){
        sum+=a[i];
    }
    float avg=sum/len;
    cout<<avg;
}
