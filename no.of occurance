#include<iostream>
using namespace std;
int main(){
    int a[]={1,2,3,4,2,3,5,6,2,2},count=0;
    int element=2;
    int len=sizeof(a)/sizeof(a[0]);
    for(int i=0;i<len;i++){
        if(a[i]==element){
            count++;
        }
    }
    cout<<"number of occurence= "<<count;
}
