#include<iostream>
using namespace std;
int main(){
    int a[]={3,4,1,2,6};
    int len=sizeof(a)/sizeof(a[0]);
    int max=a[0];
    for(int i=1;i<len;i++){
        if(a[i]>max){
           max=a[i]; 
        }
    }
    cout<<max;
}
