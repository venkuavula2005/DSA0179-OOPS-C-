#include<iostream>
using namespace std;
int main(){
    int a[3],n,i,r,sum=0;
    int len=sizeof(a)/sizeof(a[0]);
    for(i=0;i<3;i++){
        cin>>a[i];
        sum=sum+a[i];
        
    }
    cout<<sum<<endl;
    cout<<sum/len;
    
}
